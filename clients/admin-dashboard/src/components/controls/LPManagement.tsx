import { Card } from '@/components/shared/Card';

export const LPManagement = () => {
  return (
    <Card title="Liquidity Provider Management">
      <p className="text-gray-600 dark:text-gray-400">
        Configure and manage liquidity provider connections, credentials, and settings.
      </p>
    </Card>
  );
};
