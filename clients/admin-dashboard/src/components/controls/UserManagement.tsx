import { Card } from '@/components/shared/Card';

export const UserManagement = () => {
  return (
    <Card title="User Management">
      <p className="text-gray-600 dark:text-gray-400">
        Manage user accounts, roles, permissions, and risk profiles.
      </p>
    </Card>
  );
};
